//src/lib/types/project.ts

// TODO(project_page): Figure out what properties each project needs 
// create a type for each of those projects 
export type ACMProject = {
    id: string,
    projectName: string
}